## Codebase Patterns

- **Server entry:** `server/src/index.ts` exports `app` (Express instance) for testability
- **Test framework:** Vitest in server/, tests in `server/tests/`
- **Client build:** `tsc -b && vite build` (project references)
- **Server build:** `tsc` (standard compilation)
- **Monorepo:** npm workspaces with `server` and `client` packages
- **Express version:** Express v5 (no need for @types/express body parsing)
- **DB layer:** `server/src/db/` — one file per entity, `schema.ts` for init, `types.ts` for interfaces, `index.ts` re-exports all
- **DB tests:** Use `createDatabase(tempPath)` for isolated test DBs; snake_case columns mapped to camelCase interfaces
- **Route files:** `server/src/routes/` — one file per resource, named export of Router (e.g., `projectsRouter`)
- **API integration tests:** Use `supertest` with `process.env.DB_PATH` set before importing app; cleanup with `closeDatabase()` + `unlinkSync`
- **Client routing:** React Router v7 with `BrowserRouter`; pages in `client/src/pages/`; named exports
- **Google Maps:** `@vis.gl/react-google-maps` with `<APIProvider apiKey={...}>` + `<Map>` pattern; key from `VITE_GOOGLE_MAPS_API_KEY`
- **Scraper modules:** `server/src/scraper/` — keep API clients isolated per provider (e.g., `places-api.ts`) and return parsed DB-ready payloads instead of raw API JSON
- **Tiling module structure:** keep `server/src/scraper/tiling.ts` split into pure bounds math (`generateTiles`, `subdivideTile`, `shouldSubdivide`) and thin DB-backed progress helpers (`initializeTilesForRun`, `markTileCompleted`, `subdivideTileInRun`, `getRunTileProgress`)
- **Scrape engine + counters:** when orchestrating runs in `server/src/scraper/engine.ts`, create/subdivide/complete tiles via tiling helpers instead of raw tile CRUD so `scrape_runs.tiles_*` counters stay in sync
- **Scrape route orchestration:** keep background scrape state (`pauseRequested`, in-flight task handles, SSE subscribers) module-scoped in `server/src/routes/scrape.ts`, and expose lightweight test hooks (`set...ForTests`, `reset...ForTests`) to make async integration tests deterministic
- **Places API requests:** Use `X-Goog-Api-Key` + `X-Goog-FieldMask` headers and wrap network calls in `Effect.tryPromise` with typed `ScrapeError`
- **Website classification:** Normalize website hostnames with URL parsing (add `https://` fallback for protocol-less inputs), then classify via suffix matching so `www.booking.com` and other subdomains map to the base domain allowlist
- **Place IDs:** Use Google Place IDs from Places API (`place.id` or resource name `places/{id}`), and persist canonical map links in `google_maps_uri`
- **Effect-TS:** All DB functions return `Effect<A, DbError | NotFoundError>`. Routes use `Effect.runPromise` at boundary. Scraper uses `Effect.runSync` for DB calls since better-sqlite3 is synchronous. Typed errors in `server/src/errors.ts`: `DbError`, `NotFoundError`, `ValidationError`, `ScrapeError`. Schema validation via `Schema.decodeUnknown` from `effect` package.

---

## 2026-02-23 - US-001
Thread: https://ampcode.com/threads/T-019c8c87-3bac-7712-94cc-7f06fe88cd13
- Set up npm workspaces monorepo with server/ and client/ packages
- Moved MVP code to legacy/src/index.ts
- Server: Express v5 + TypeScript with /health endpoint
- Client: Vite + React + TypeScript with placeholder page
- Created .env.example with GOOGLE_MAPS_API_KEY placeholders
- Vite proxy forwards /api/* to Express server
- Files changed: package.json, tsconfig.json, .gitignore, .env.example, server/*, client/*, legacy/src/index.ts
- **Learnings for future iterations:**
  - Express v5 is latest stable; use `express` directly (not express@4)
  - Client uses project references tsconfig (tsconfig.json → tsconfig.app.json)
  - Server test imports use `.js` extension for ESM compatibility
  - `npx --workspace=server tsx src/index.ts` runs server from root (paths relative to workspace)
---

## 2026-02-23 - US-002
Thread: https://ampcode.com/threads/T-019c8c8b-feed-721a-a9c8-8689742c3686
- Created SQLite schema with all 8 tables: projects, scrape_runs, tiles, places, reviews, place_scrape_runs, shortlists, shortlist_entries
- Typed CRUD functions for each entity in server/src/db/
- Tests verify schema creation, CRUD for projects and places, cascade deletes, unique constraints
- Files changed: server/src/db/ (10 files), server/tests/db.test.ts, server/package.json
- **Learnings for future iterations:**
  - better-sqlite3 is synchronous — no async needed for DB calls
  - Use `Record<string, unknown>` for raw row typing, then map to typed interfaces
  - SQLite column names use snake_case, TypeScript interfaces use camelCase — map in dedicated functions
  - DB path is configurable via DB_PATH env var (default: data/gomaps.db)
  - `createDatabase()` can be used directly in tests with temp paths for isolation
  - Foreign keys must be enabled per-connection with `db.pragma('foreign_keys = ON')`
---

## 2026-02-23 - US-003
Thread: https://ampcode.com/threads/T-019c8c8f-b08c-7348-997c-e18b89c71974
- Created Express route modules: projects (full CRUD), scrape, places, shortlists (placeholders)
- Mounted routes at /api/projects, /api/scrape, /api/places, /api/shortlists
- 14 integration tests using supertest covering all CRUD operations + error cases + placeholder routes
- Files changed: server/src/index.ts, server/src/routes/{projects,scrape,places,shortlists}.ts, server/tests/projects.test.ts, server/package.json
- **Learnings for future iterations:**
  - supertest works well with Express v5 — pass the app directly, no need to start/stop server
  - Set `process.env.DB_PATH` before importing app to redirect the DB singleton to a temp file for tests
  - Express v5 route params accessed via `req.params.id` (same as v4)
  - Placeholder routers just need a single GET / returning empty array
---

## 2026-02-23 - US-004
Thread: https://ampcode.com/threads/T-019c8cb2-c300-74eb-acd0-66e4d3352f21
- Bootstrapped React app with React Router (Setup, Explorer, Shortlists pages)
- Embedded Google Maps on Setup page using @vis.gl/react-google-maps
- API key loaded from VITE_GOOGLE_MAPS_API_KEY env var
- Vite proxy already configured from US-001 (forwards /api/* to Express)
- Files changed: client/src/App.tsx, client/src/pages/{SetupPage,ExplorerPage,ShortlistsPage}.tsx, client/package.json
- **Learnings for future iterations:**
  - `@vis.gl/react-google-maps` uses `<APIProvider>` + `<Map>` components — simple to embed
  - NavLink from react-router-dom provides `isActive` for styling active nav links
  - Use `gestureHandling="greedy"` on Map for full pan/zoom without modifier keys
  - Pages live in `client/src/pages/` with named exports (e.g., `SetupPage`)
---

## 2026-02-24 - US-005
Thread: https://ampcode.com/threads/T-019c8cbc-7385-742a-8306-65e4909e615f
- Extracted MVP scraper logic into modular `server/src/scraper/engine.ts`
- Created `server/src/scraper/utils.ts` with pure utility functions (normalizePlaceUrl, parseLatLngFromUrl, generatePlaceId)
- `startScrape(config)` accepts typed ScrapeConfig and writes to SQLite via DB layer
- Places inserted via `createPlace()`, reviews via `createReview()`, junction via `linkPlaceToScrapeRun()`
- CAPTCHA detection preserved as polling mechanism (checks every 3s, 5min timeout)
- Checkpoint/resume: existing places detected via `getPlace()` and skipped
- Cookie consent handling preserved from legacy
- Files changed: server/src/scraper/engine.ts, server/src/scraper/utils.ts, server/tests/scraper-utils.test.ts, server/package.json
- **Learnings for future iterations:**
  - Playwright installed as dependency (browsers installed separately via `npx playwright install`)
  - CAPTCHA handling changed from readline (interactive) to polling (non-interactive) since engine runs as background task
  - Place ID is SHA-256 hash of canonical URL (first 16 hex chars) — deterministic dedup
  - `createReview()` takes `relativeDate?: string` (optional), but scraper data has `string | null` — use `?? undefined`
  - Legacy code had `topReviews: string[]` (just text); new engine extracts individual rating + relativeDate per review
---

## 2026-02-24 - US-025
Thread: https://ampcode.com/threads/T-019c91b1-6714-730b-a164-c586f5713c65
- Migrated entire server/ package from plain TypeScript to Effect-TS
- Installed `effect` and `@effect/schema` (deprecated, merged into `effect` 3.x)
- Created `server/src/errors.ts` with typed errors: DbError, NotFoundError, ValidationError, ScrapeError (using `Data.TaggedError`)
- All DB functions (projects, places, reviews, scrape-runs, tiles, place-scrape-runs, shortlists) now return `Effect<A, DbError | NotFoundError>`
- Express route handlers in `routes/projects.ts` use `Effect.runPromise` at the API boundary with `Effect.catchTag` for error handling
- API request validation uses `Schema.Struct` + `Schema.decodeUnknown` from `effect` package (replaced manual `if (!name)` checks)
- Scraper `startScrape()` returns `Effect<void, ScrapeError>` wrapping async implementation
- Scraper internally uses `Effect.runSync()` for DB calls (safe since better-sqlite3 is synchronous)
- Files changed: server/src/errors.ts (new), server/src/db/*.ts (all 8 modules), server/src/routes/projects.ts, server/src/scraper/engine.ts, server/package.json
- **Learnings for future iterations:**
  - `@effect/schema` is deprecated in Effect 3.x — use `import { Schema } from "effect"` directly
  - `Data.TaggedError("Tag")<{ ... }>` creates errors with `_tag` property for pattern matching via `Effect.catchTag`
  - `Effect.try({ try, catch })` wraps synchronous operations that may throw
  - `Effect.tryPromise({ try, catch })` wraps async operations
  - `Effect.gen(function* () { ... })` composes multiple effects with `yield*`
  - `Effect.runSync()` executes sync effects; throws on failure (same behavior as original sync DB calls)
  - `Effect.runPromise()` executes effects (sync or async) returning a Promise
  - For "find or undefined" patterns: `getX(id).pipe(Effect.catchTag("NotFoundError", () => Effect.succeed(undefined as undefined)))`
---

## 2026-02-25 23:33:48 CET - US-005
Thread: https://ampcode.com/threads/T-019c96ec-8860-70cf-a1f1-cb65da4ea78f
- Replaced Playwright scraper artifacts with a dedicated Places API client module at `server/src/scraper/places-api.ts`
- Implemented `textSearch()` and `getPlaceDetails()` as Effect-based HTTP wrappers with field masks, API key headers, pagination token support, and response mapping into `CreatePlaceInput`
- Renamed `Place.googleUrl` to `Place.googleMapsUri` across DB types/mappers and schema column (`google_url` → `google_maps_uri`), and aligned DB tests accordingly
- Removed legacy scraper files (`server/src/scraper/engine.ts`, `server/src/scraper/utils.ts`) and old utility tests
- Added unit tests with mocked `fetch` for both Places endpoints (`server/tests/places-api.test.ts`)
- Files changed: `server/src/scraper/places-api.ts`, `server/src/db/{types,places,schema,index}.ts`, `server/tests/{db,places-api}.test.ts`, `server/package.json`, `prd.json`, `progress.txt`
- **Learnings for future iterations:**
  - Keep Places field masks explicit and test for required fields to avoid accidental SKU creep
  - Parse Place ID from both `id` and `name=places/{id}` because API payloads vary between endpoints
  - Canonical map links should be persisted as `google_maps_uri` to match Places API naming and downstream UI expectations
---

## 2026-02-25 23:37:52 CET - US-006
Thread: https://ampcode.com/threads/T-019c96f1-762b-775a-abea-cc7bfcf3863f
- Implemented adaptive tiling module in `server/src/scraper/tiling.ts` with coarse grid generation, 4-way subdivision, minimum tile floor handling (0.01°), and threshold helper defaulting to 60 results
- Added DB-backed tile progress helpers to initialize run tiles, mark tile state transitions (running/completed/subdivided), persist subdivision children, and query run progress (`tilesTotal`, `tilesCompleted`, `tilesSubdivided`, `placesFound`)
- Added unit/integration coverage in `server/tests/tiling.test.ts` for bounds math, subdivision behavior, threshold logic, and SQLite-backed progress/status tracking
- Files changed: `server/src/scraper/tiling.ts`, `server/tests/tiling.test.ts`, `AGENTS.md`, `prd.json`, `progress.txt`
- **Learnings for future iterations:**
  - Keep tiling computations pure and side-effect free, then layer persistence as separate Effect helpers for easier testing
  - Use `ManagedRuntime.make(DbLive(tempDbPath))` in tests to validate Effect-based DB modules against isolated SQLite files
  - When subdividing, update parent tile status and scrape-run aggregate counters in the same effect flow to avoid drift in progress metrics
---

## 2026-02-25 23:42:53 CET - US-007
Thread: https://ampcode.com/threads/T-019c96f4-c54b-7769-8ccf-432720f1de9a
- Implemented the Places API scrape orchestrator in `server/src/scraper/engine.ts` with tile-queue processing, up-to-3-page pagination, subdivision at 60+ hits, dedup/linking by Google Place ID, detail enrichment, and run lifecycle updates (`running` → `completed` / `failed`)
- Added engine tests in `server/tests/engine.test.ts` covering subdivision + pagination flow, checkpoint/resume from existing tile states, and existing-place dedup behavior (link without re-fetching details)
- Updated reusable guidance in `AGENTS.md`, set `US-007` to passing in `prd.json`, and validated required checks
- Files changed: `server/src/scraper/engine.ts`, `server/tests/engine.test.ts`, `AGENTS.md`, `prd.json`, `progress.txt`
- **Learnings for future iterations:**
  - In this codebase, `placesFound` tracks raw processed hits from completed tiles (including duplicates across pages), while `placesUnique` tracks newly inserted place IDs
  - `startScrape()` should treat both `pending` and `running` tiles as runnable to support interruption-safe resume semantics
  - Direct `createTile()` calls do not update `scrape_runs.tiles_total`; use tiling helpers when aggregate counters matter
---

## 2026-02-26 22:34:31 CET - US-008
Thread: https://ampcode.com/threads/T-019c9bde-9b0f-7021-8c45-4d8182a600c6
- Implemented website classification module in `server/src/scraper/classifier.ts` with hardcoded OTA/social allowlists and robust hostname normalization for protocol-less inputs and subdomains
- Wired classifier into `server/src/scraper/places-api.ts` so parsed place payloads persist `websiteType` from `classifyWebsite()` instead of defaulting to `unknown`
- Added comprehensive tests in `server/tests/classifier.test.ts` (15+ cases) covering OTA, social, direct, subdomain, null/undefined, and invalid URL edge cases
- Updated existing Places API tests to assert classified `websiteType` values
- Files changed: `server/src/scraper/classifier.ts`, `server/src/scraper/places-api.ts`, `server/tests/classifier.test.ts`, `server/tests/places-api.test.ts`, `AGENTS.md`, `prd.json`, `progress.txt`
- **Learnings for future iterations:**
  - Keep classification centralized in one scraper helper and reuse it during API response mapping so DB writes stay consistent across Text Search and Place Details flows
  - Domain suffix matching against normalized hostnames is enough for OTA/social heuristics without needing per-path rules
  - Dedicated classifier unit tests catch subtle hostname parsing regressions early (uppercase hosts, protocol-less input, malformed URLs)
---

## 2026-02-26 22:40:54 CET - US-009
Thread: https://ampcode.com/threads/T-019c9be1-3b37-7184-9bb5-33b69f8f8702
- Implemented full scrape API orchestration in `server/src/routes/scrape.ts`: start run, status endpoint, pause/resume controls, and SSE progress stream using background task management
- Extended `server/src/scraper/engine.ts` with optional `shouldPause` and `onProgress` hooks so route-level orchestration can pause gracefully between tiles and broadcast typed progress snapshots
- Added integration coverage in `server/tests/projects.test.ts` for start/status, pause/resume lifecycle, and SSE payload streaming with deterministic mock executors
- Files changed: `server/src/routes/scrape.ts`, `server/src/scraper/engine.ts`, `server/tests/projects.test.ts`, `AGENTS.md`, `prd.json`, `progress.txt`
- **Learnings for future iterations:**
  - Keep long-running scrape control state in-memory at the route boundary while preserving durable progress/status in `scrape_runs`
  - Exposing small injectable executor hooks in route modules is an effective way to test background async workflows without hitting external APIs
  - For pause semantics, return `202 pausing` immediately and let the worker mark durable `paused` state at the next safe checkpoint
---
## 2026-02-26 22:47:43 CET - US-010
Thread: https://ampcode.com/threads/T-019c9be9-89ea-719e-b4bd-faabaa2f1da5
- Implemented a dedicated Projects management UI page with a dark themed nav/header, responsive project card grid, selection glow state, empty-state card, and map preview blocks with bounds overlays.
- Added client-side project API helpers and wired create/delete flows: `+ New Project` toggles a creation form that POSTs to `/api/projects`, delete uses confirmation + refresh, and cards navigate to `/projects/:id/setup`.
- Updated app routing so Projects is the default landing page and setup now supports project-specific paths.
- Files changed: `client/src/App.tsx`, `client/src/main.tsx`, `client/src/pages/ProjectsPage.tsx`, `client/src/pages/SetupPage.tsx`, `client/src/lib/api.ts`, `client/src/styles.css`, `prd.json`, `progress.txt`
- **Learnings for future iterations:**
  - For pre-mockup UI stories, it helps to centralize API calls in `client/src/lib/api.ts` first so page components stay focused on state/rendering and later stories can reuse the same CRUD helpers.
  - Project cards can render lightweight map previews with a CSS bounds overlay even before full drawing/edit flows are implemented in Setup.
  - Keep `Projects` as the router default once project selection becomes the primary entry point for downstream pages.
---
