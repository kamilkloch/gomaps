{
  "project": "GoMaps",
  "branchName": "ralph/gomaps-v1",
  "description": "Google Maps accommodation discovery tool using Google Places API (New) for data fetching, with interactive Explorer UI — see SPEC.md for full specification, DEVELOPMENT.md for implementation guide",
  "userStories": [
    {
      "id": "US-001",
      "title": "Project restructure — monorepo setup",
      "description": "Set up monorepo with server/ and client/ packages. Move existing MVP to legacy/. Server: Express + TypeScript. Client: Vite + React + TypeScript. Root npm workspaces. .env.example with GOOGLE_MAPS_API_KEY.",
      "acceptanceCriteria": [
        "npm install from root succeeds",
        "server/ and client/ each have package.json and tsconfig.json",
        "legacy/src/index.ts contains the original MVP code",
        "npm run typecheck passes in both server/ and client/",
        "npm run build succeeds in both packages",
        "server starts and responds to GET /health with 200",
        "client dev server starts and renders a placeholder page",
        ".env.example exists with GOOGLE_MAPS_API_KEY placeholder"
      ],
      "priority": 1,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-002",
      "title": "SQLite database schema and data access layer",
      "description": "Create SQLite schema covering all entities from SPEC.md §6: Project, ScrapeRun, Tile, Place, Review, PlaceScrapeRun, Shortlist, ShortlistEntry. Use better-sqlite3. Typed query functions for CRUD. Tests for schema creation and basic CRUD.",
      "acceptanceCriteria": [
        "better-sqlite3 installed in server/",
        "Schema creates all tables: projects, scrape_runs, tiles, places, reviews, place_scrape_runs, shortlists, shortlist_entries",
        "Typed CRUD functions exist for each entity",
        "Tests pass: create DB, insert/read/update/delete for Project and Place entities",
        "npm run typecheck passes in server/",
        "Database file is created in a configurable location (default: data/gomaps.db)"
      ],
      "priority": 2,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-003",
      "title": "Express API skeleton with project CRUD",
      "description": "Express server with route modules for projects, scrapes, places, shortlists. Full CRUD for Projects endpoint. Other routes export placeholder routers. Integrate with SQLite DB layer from US-002.",
      "acceptanceCriteria": [
        "GET /api/projects returns list of projects",
        "POST /api/projects creates a project (name, bounds)",
        "GET /api/projects/:id returns single project",
        "PUT /api/projects/:id updates project",
        "DELETE /api/projects/:id deletes project",
        "Placeholder routers mounted at /api/scrape, /api/places, /api/shortlists",
        "npm run typecheck passes",
        "Integration tests pass for project CRUD"
      ],
      "priority": 3,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-004",
      "title": "React app shell with Google Maps",
      "description": "Bootstrap React app with Vite. React Router with pages: Setup, Explorer, Shortlists. Embed Google Maps on Setup page using @vis.gl/react-google-maps. Map renders, is interactive. Proxy API requests to Express server.",
      "acceptanceCriteria": [
        "npm run dev starts the React dev server",
        "React Router navigates between Setup, Explorer, Shortlists pages",
        "Google Maps renders on Setup page (interactive — pan, zoom)",
        "API key loaded from VITE_GOOGLE_MAPS_API_KEY env var",
        "Vite proxy forwards /api/* requests to Express server",
        "npm run build succeeds in client/",
        "Typecheck passes"
      ],
      "priority": 4,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-025",
      "title": "Migrate server TypeScript to Effect-TS",
      "description": "Migrate the server/ package from plain TypeScript to Effect. Replace try/catch with typed Effect errors, wrap DB access in Effect services with Layer-based dependency injection, use Effect.gen for async flows, and adopt Effect Schema for API request/response validation.",
      "acceptanceCriteria": [
        "effect installed in server/",
        "DB access layer (server/src/db/) wrapped as Effect services with typed errors (DbError, NotFoundError)",
        "Express route handlers use Effect.runPromise at the API boundary",
        "API request validation uses Effect Schema instead of manual checks",
        "All existing API endpoints preserved (no regressions)",
        "npm run typecheck --workspace=server passes",
        "npm test --workspace=server passes"
      ],
      "priority": 5,
      "passes": true,
      "notes": "Effect-TS v3.19.19 installed. All DB functions return Effect<A, DbError | NotFoundError, Db>. Typed errors in server/src/errors.ts."
    },
    {
      "id": "US-005",
      "title": "Places API client module and Playwright removal",
      "description": "Replace the Playwright-based scraper with a Google Places API (New) HTTP client. Create server/src/scraper/places-api.ts wrapping Text Search (POST /v1/places:searchText) and Place Details (GET /v1/places/{placeId}). Use field masks to control SKU costs. Parse API responses into DB types. Remove Playwright dependency. Rename Place.googleUrl to Place.googleMapsUri throughout codebase (types, DB mapper, schema column, routes, tests). Place IDs now use Google Place ID (e.g., 'ChIJ...'). Remove old engine.ts and utils.ts. Use Effect patterns consistent with existing codebase.",
      "acceptanceCriteria": [
        "server/src/scraper/places-api.ts exports textSearch() and getPlaceDetails() functions returning Effect types",
        "textSearch() calls POST https://places.googleapis.com/v1/places:searchText with API key, query, locationBias, and field mask",
        "getPlaceDetails() calls GET https://places.googleapis.com/v1/places/{placeId} with API key and field mask",
        "API responses parsed into CreatePlaceInput shape for DB insertion",
        "Field masks configured to request: displayName, location, rating, userRatingCount, formattedAddress, internationalPhoneNumber, websiteUri, primaryTypeDisplayName, priceLevel, googleMapsUri, reviews, photos, regularOpeningHours",
        "Old server/src/scraper/engine.ts (Playwright) removed",
        "Old server/src/scraper/utils.ts removed",
        "playwright removed from server/package.json dependencies",
        "Place.googleUrl renamed to Place.googleMapsUri in types.ts, places.ts DB functions, schema.ts (column rename: google_url → google_maps_uri), route files, and tests",
        "Place ID uses Google Place ID format instead of SHA-256 hash",
        "Unit tests pass with mocked HTTP responses for both textSearch and getPlaceDetails",
        "npm run typecheck --workspace=server passes",
        "npm test --workspace=server passes"
      ],
      "priority": 6,
      "passes": true,
      "notes": "Use native fetch() for HTTP calls. API key from process.env.GOOGLE_MAPS_API_KEY. Wrap in Effect.tryPromise for error handling. See SPEC.md §7 for field mapping table."
    },
    {
      "id": "US-006",
      "title": "Adaptive tiling algorithm",
      "description": "Implement server/src/scraper/tiling.ts. Given bounding box, generate coarse grid (~0.1°). Process tiles sequentially. If Text Search result count >= 60 (API pagination limit), subdivide into 4 sub-tiles. Track tile status in DB. Enforce minimum tile size floor. See SPEC.md §5.",
      "acceptanceCriteria": [
        "generateTiles(bounds) returns initial grid of tile objects",
        "subdivideTile(tile) returns 4 child tiles",
        "shouldSubdivide(resultCount, threshold) returns boolean with default threshold of 60",
        "Minimum tile size floor (0.01°) prevents infinite recursion",
        "Tile status tracked in SQLite: pending, running, completed, subdivided",
        "Progress can be queried: tiles total/completed/subdivided, places found",
        "Unit tests pass for tile generation, subdivision, bounds math",
        "npm run typecheck --workspace=server passes",
        "npm test --workspace=server passes"
      ],
      "priority": 7,
      "passes": true,
      "notes": "Threshold is 60 (API pagination limit of 3 pages × 20 results), not 120 (old browser scraping limit)."
    },
    {
      "id": "US-007",
      "title": "Scrape engine orchestrator using Places API",
      "description": "Implement server/src/scraper/engine.ts as the orchestrator that combines tiling + Places API client + DB layer. For each tile: call Text Search with locationBias, paginate results (up to 3 pages), check subdivision threshold, insert/update places in DB. Then optionally fetch Place Details for enriched data (reviews, photos). De-duplicate by Google Place ID. Checkpoint/resume via tile status. Rate limiting between requests.",
      "acceptanceCriteria": [
        "server/src/scraper/engine.ts exports a startScrape(config) function returning Effect",
        "Config accepts: scrapeRunId, query, bounds, optional delayMs (default 200ms)",
        "Engine generates tiles for bounds, iterates tiles sequentially",
        "For each tile: calls textSearch with locationBias, paginates via nextPageToken",
        "Places inserted into DB via createPlace(), reviews via createReview()",
        "De-duplication: existing places (by Google Place ID) are linked but not re-scraped",
        "Subdivision: tiles with 60+ results are subdivided into 4 sub-tiles",
        "Tile status updated in DB after each tile (checkpoint/resume)",
        "ScrapeRun progress updated: placesFound, placesUnique, tilesCompleted",
        "Configurable delay between API requests for rate limiting",
        "Website classification applied to each place via classifier (US-008 dependency — skip if not yet implemented)",
        "npm run typecheck --workspace=server passes",
        "npm test --workspace=server passes"
      ],
      "priority": 8,
      "passes": true,
      "notes": "This replaces the old Playwright-based engine. Uses Effect patterns. Depends on US-005 (places-api.ts) and US-006 (tiling.ts)."
    },
    {
      "id": "US-008",
      "title": "Website classification",
      "description": "Implement server/src/scraper/classifier.ts. Classify place website domain as direct/ota/social/unknown. Hardcoded allowlist of ~30 OTA and social domains. See SPEC.md §7 classification table.",
      "acceptanceCriteria": [
        "classifyWebsite(url) returns 'direct' | 'ota' | 'social' | 'unknown'",
        "Correctly classifies: booking.com, airbnb.com, expedia.com, hotels.com, vrbo.com, agoda.com, tripadvisor.com → ota",
        "Correctly classifies: facebook.com, instagram.com → social",
        "Correctly classifies: www.hotel-example.it → direct",
        "Handles subdomains: www.booking.com → ota",
        "Handles null/undefined input → unknown",
        "Place records include websiteType field populated by classifier",
        "Comprehensive tests pass (15+ test cases)",
        "npm run typecheck --workspace=server passes",
        "npm test --workspace=server passes"
      ],
      "priority": 9,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-009",
      "title": "Scrape API — start, monitor, pause with SSE progress",
      "description": "Wire scrape engine into Express API. POST to start scrape (projectId + query). GET scrape run status. POST to pause/resume. SSE endpoint for live progress. Engine runs as background async task in server process.",
      "acceptanceCriteria": [
        "POST /api/scrape/start accepts { projectId, query } and returns scrapeRunId",
        "GET /api/scrape/:runId returns run status, tile progress, place counts",
        "POST /api/scrape/:runId/pause pauses the running scrape",
        "POST /api/scrape/:runId/resume resumes a paused scrape",
        "GET /api/scrape/:runId/progress is an SSE endpoint streaming progress events",
        "Progress events include: { tilesCompleted, tilesTotal, tilesSubdivided, placesFound, placesUnique, elapsedMs }",
        "Scrape runs in background — API remains responsive during scrape",
        "npm run typecheck --workspace=server passes",
        "npm test --workspace=server passes"
      ],
      "priority": 10,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-010",
      "title": "Project management UI",
      "description": "React Projects page matching mockup at docs/mockups/01-projects-page.png. Dark theme. Top nav bar with GoMaps logo, nav links (Projects/Explorer/Shortlists/Settings), user avatar. Responsive 3-column card grid. Each card: project title, status badge (Complete=green, Running=amber, Draft=gray), embedded Google Maps preview with blue polygon boundary and markers, footer metadata (places count, scrape runs, last scraped). Selected card has blue glow border. Empty state card with dashed blue border and '+' icon. '+ New Project' button in header.",
      "acceptanceCriteria": [
        "Projects page lists all projects from API as card grid (3 columns, responsive)",
        "Top nav bar: GoMaps logo, Projects/Explorer/Shortlists/Settings links, user avatar",
        "Each card shows: project title, status badge (Complete/Running/Draft with color coding), map preview with project bounds polygon, metadata footer (places count, scrape runs count, last scraped time)",
        "Selected/hovered card has blue glow border",
        "Empty state card with dashed blue border, '+' icon, and 'Create your first project' text",
        "'+ New Project' button in page header opens creation form",
        "Creating a project calls POST /api/projects and refreshes list",
        "Clicking a project navigates to /projects/:id/setup",
        "Delete button removes project (with confirmation)",
        "Dark theme styling matching mockup color palette (deep navy background, dark slate cards)",
        "npm run build --workspace=client succeeds",
        "npm run typecheck --workspace=client passes"
      ],
      "priority": 11,
      "passes": true,
      "notes": "Reference mockup: docs/mockups/01-projects-page.png"
    },
    {
      "id": "US-011",
      "title": "Google Maps area selection with drawable rectangle",
      "description": "On project Setup page matching mockup at docs/mockups/02-scrape-setup-page.png. Left panel: dark-themed Google Maps with blue-bordered selection rectangle with circular drag handles at corners. Floating coordinates pill at bottom center showing 'SW: lat°N, lng°E — NE: lat°N, lng°E'. Right sidebar: 'Scrape Area' section with 'Select Area' (blue) and 'Clear' (dark gray with trash icon) buttons.",
      "acceptanceCriteria": [
        "Setup page has two-panel layout: large map (left) and configuration sidebar (right)",
        "Breadcrumbs in header: Projects > [Project Name] > Setup",
        "'Select Area' button (blue, with cursor icon) creates a rectangle on map",
        "Rectangle has blue border with circular drag handles at corners for resizing",
        "Rectangle is draggable and resizable",
        "Floating coordinates pill at bottom center: 'SW: lat°N, lng°E — NE: lat°N, lng°E'",
        "'Clear' button (dark gray, trash icon) removes the selection",
        "Bounds saved to project via PUT /api/projects/:id",
        "On page load, existing project bounds shown as rectangle on map",
        "npm run typecheck --workspace=client passes"
      ],
      "priority": 12,
      "passes": true,
      "notes": "Reference mockup: docs/mockups/02-scrape-setup-page.png"
    },
    {
      "id": "US-012",
      "title": "Scrape launch and live progress display",
      "description": "Query input, start button, tile overlay, and progress panel on Setup page sidebar matching mockup at docs/mockups/02-scrape-setup-page.png. Sidebar has: query input with magnifying glass icon, estimate badge ('~52 tiles · Est. 25 min'), large green 'Start Scrape' button with play icon, 'Previous Runs' list with status/metrics, and progress section with striped animated progress bar, percentage, and detailed stats (tiles, places with unique count, elapsed/remaining time). Map shows tile grid overlay: green=completed, yellow=active, gray=pending.",
      "acceptanceCriteria": [
        "Query text input in sidebar with magnifying glass icon and placeholder text",
        "Estimate badge below query showing estimated tile count and time",
        "'Start Scrape' button: large green with play icon and outer glow",
        "'Previous Runs' section listing past runs with query name, status badge (green checkmark + 'Complete'), places count, and time elapsed",
        "Progress section: striped animated progress bar with percentage",
        "Detailed stats below progress bar: tiles (completed/total), places (total + unique count), time (elapsed + estimated remaining)",
        "Tile grid overlay on map: green tiles (completed), yellow tile (active), dark gray tiles (pending)",
        "Pause/Resume button toggles scrape state",
        "Progress subscribes to SSE for live updates",
        "npm run typecheck --workspace=client passes"
      ],
      "priority": 13,
      "passes": true,
      "notes": "Reference mockup: docs/mockups/02-scrape-setup-page.png"
    },
    {
      "id": "US-013",
      "title": "Map view with place markers and clustering",
      "description": "Explorer page matching mockup at docs/mockups/03-explorer-page.png. Four-pane layout: header with project selector and search bar, map (top-left), detail panel (center-right), data table (bottom), filter sidebar (far-right). Dark-mode vector map with colored dot markers (green/red/yellow by rating) and blue cluster circles with white numeric counts. Selected place has pulse effect with green translucent radius.",
      "acceptanceCriteria": [
        "Explorer page at /projects/:id/explorer with four-pane layout per mockup",
        "Header: GoMaps logo, project selector dropdown, centered search bar, 'Filters' button with active filter count badge",
        "Dark-mode styled Google Maps with simplified road networks",
        "Places rendered as colored dot markers: green (> 4.2), yellow (3.5–4.2), red (< 3.5)",
        "Blue cluster circles with white numeric counts when zoomed out (via @googlemaps/markerclusterer)",
        "Selected marker has pulse effect with green translucent radius highlight",
        "npm run typecheck --workspace=client passes"
      ],
      "priority": 14,
      "passes": true,
      "notes": "Reference mockup: docs/mockups/03-explorer-page.png"
    },
    {
      "id": "US-014",
      "title": "Place table with sorting and virtual scrolling",
      "description": "Bottom panel data table on Explorer page per mockup at docs/mockups/03-explorer-page.png. Columns: Name, Category, Rating, Reviews, Price ($/$$/$$$), Website (with 'OTA' or 'Direct' status badges), Address, Favorite (star icon). Highlighted row for selected item. Filter input and Clear button above table. Horizontal scrollbar.",
      "acceptanceCriteria": [
        "Table in bottom panel displays all places for current project",
        "Columns: Name, Category, Rating, Reviews, Price ($/$$/$$$), Website (with 'OTA'/'Direct' badge), Address, Favorite (star icon)",
        "Clicking column header sorts ascending/descending",
        "Website column shows status badge: 'OTA' (orange/blue) or 'Direct' (green)",
        "Selected/highlighted row corresponds to selected place on map",
        "Virtual scrolling handles 5000+ rows without lag",
        "'Filter results...' text input and 'Clear' button above table",
        "Horizontal scrollbar at bottom",
        "npm run typecheck --workspace=client passes"
      ],
      "priority": 15,
      "passes": false,
      "notes": "Reference mockup: docs/mockups/03-explorer-page.png"
    },
    {
      "id": "US-015",
      "title": "Detail panel with reviews",
      "description": "Center-right detail panel on Explorer page per mockup at docs/mockups/03-explorer-page.png. Shows: title, category, star rating with review count, 'Book Direct' badge (green), contact info (website link, address with pin, phone with phone icon, price indicator $$), amenities as horizontal tags, photo gallery thumbnails, 'Opening hours' accordion, scrapedAt timestamp, reviews list with star ratings, action buttons (Open in Google Maps, Search on Booking.com, Search on Airbnb).",
      "acceptanceCriteria": [
        "Panel opens on right side when a place is selected",
        "Header: place title, category label, star rating (yellow), review count",
        "'Book Direct' badge (green) or 'OTA' badge based on website type",
        "Contact section: clickable website link, address with pin icon, phone as tel: link, price indicator ($/$$/$$$)",
        "Amenities as horizontal row of dark blue rounded tag chips",
        "Photo gallery: row of rectangular thumbnail images",
        "'Opening hours' expandable accordion section",
        "scrapedAt timestamp metadata",
        "Reviews list with star ratings and text snippets",
        "Three full-width blue action buttons: 'Open in Google Maps', 'Search on Booking.com', 'Search on Airbnb'",
        "npm run typecheck --workspace=client passes"
      ],
      "priority": 16,
      "passes": false,
      "notes": "Reference mockup: docs/mockups/03-explorer-page.png"
    },
    {
      "id": "US-016",
      "title": "Map ↔ Table bidirectional selection sync",
      "description": "Clicking map marker highlights table row + opens detail panel. Clicking table row centers map on marker + opens detail panel. Selected marker has distinct visual style.",
      "acceptanceCriteria": [
        "Clicking a map marker: scrolls table to corresponding row, highlights it, opens detail panel",
        "Clicking a table row: centers map on corresponding marker, opens detail panel",
        "Selected marker has distinct visual style (larger/different color/bouncing)",
        "Only one place selected at a time",
        "Deselecting (clicking empty area) closes detail panel",
        "npm run typecheck --workspace=client passes"
      ],
      "priority": 17,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-017",
      "title": "Fuzzy text search",
      "description": "Search bar at top of Explorer. Fuzzy search across place name, address, category, amenities, review text. Updates map (only matching markers) and table. Use Fuse.js or SQLite FTS5. Debounce 300ms.",
      "acceptanceCriteria": [
        "Search input at top of Explorer page",
        "Typing filters places by fuzzy match across: name, address, category, amenities, review text",
        "Map shows only matching markers",
        "Table shows only matching rows",
        "Search is debounced (300ms)",
        "Clearing search restores all results",
        "npm run typecheck --workspace=client passes"
      ],
      "priority": 18,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-018",
      "title": "Filter panel",
      "description": "Far-right filter sidebar on Explorer page per mockup at docs/mockups/03-explorer-page.png. Contains: Rating range slider (3.0–5.0), Category checkbox group (Hotel, Vacation Rental, B&B, Apartment), Website radio button group (Direct Booking, OTA, Unknown). AND logic. Updates map + table live.",
      "acceptanceCriteria": [
        "Filter sidebar on far-right of Explorer page per mockup layout",
        "Rating: range slider (1.0–5.0) with blue handle and track, showing min/max values",
        "Category: checkbox group (Hotel, Vacation Rental, B&B, Apartment)",
        "Website: radio button group (Direct Booking, OTA, Unknown)",
        "Header 'Filters' button in nav bar shows badge with active filter count",
        "All filters combine with AND logic",
        "Map markers and table update live as filters change",
        "Filter state persisted in URL query parameters",
        "npm run typecheck --workspace=client passes"
      ],
      "priority": 19,
      "passes": false,
      "notes": "Reference mockup: docs/mockups/03-explorer-page.png"
    },
    {
      "id": "US-019",
      "title": "Review keyword search and distance filter",
      "description": "Two advanced filters: (1) Review keyword text input — filter to places with matching reviews, highlight keyword in detail panel. (2) Distance from point — click map to set center point, radius slider (1–50km), filter places within radius.",
      "acceptanceCriteria": [
        "Review keyword filter: text input that filters to places where >= 1 review contains the keyword",
        "Matching keywords highlighted in detail panel review section",
        "Distance filter: click map to set center point (shown as marker)",
        "Radius slider (1–50km) appears after point is set",
        "Only places within radius are shown on map and table",
        "Circle overlay drawn on map showing the radius",
        "Both filters integrate with existing filter system (AND logic)",
        "npm run typecheck --workspace=client passes"
      ],
      "priority": 20,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-020",
      "title": "Star/bookmark places and shortlist management",
      "description": "Shortlists page matching mockup at docs/mockups/04-shortlists-page.png. Left sidebar with shortlist navigation. Star toggle in Explorer table and detail panel. Full shortlist CRUD API.",
      "acceptanceCriteria": [
        "Shortlists page with left sidebar and main content area per mockup",
        "Sidebar lists shortlists with icon, name, place count; active shortlist highlighted in blue",
        "'+ Create Shortlist' button at sidebar bottom with blue outline",
        "Breadcrumbs: Projects > [Project Name] > Shortlists",
        "View title with shortlist name and place count badge",
        "Data table columns: Checkbox, Name, Rating, Reviews, Price, Website, Notes (inline text input), Actions (star icon, trash icon)",
        "Notes column has inline editable text input with placeholder '[Add a note]'",
        "Star toggle in Explorer table rows and detail panel saves to default shortlist",
        "POST/GET/PUT/DELETE /api/shortlists and /api/shortlists/:id/entries endpoints work",
        "Can create, rename, delete shortlists",
        "npm run typecheck --workspace=client passes",
        "npm run typecheck --workspace=server passes"
      ],
      "priority": 21,
      "passes": false,
      "notes": "Reference mockup: docs/mockups/04-shortlists-page.png"
    },
    {
      "id": "US-021",
      "title": "Comparison view and CSV export",
      "description": "On Shortlists page per mockup at docs/mockups/04-shortlists-page.png. Checkbox selection (max 3). 'Compare (N)' button and 'Export CSV' button. Comparison cards with place details side by side.",
      "acceptanceCriteria": [
        "Checkbox selection on shortlist table rows; selected rows show blue checkbox and highlight",
        "'Compare (N)' solid blue primary button in header showing selected count",
        "'Export CSV' outlined button with download icon in header",
        "Comparison cards (horizontal layout): thumbnail, title, star rating, review count, price, source badge, amenities chips, address",
        "Max 3 items in comparison",
        "'Export CSV' downloads shortlist as CSV with all place fields plus user notes",
        "npm run typecheck --workspace=client passes"
      ],
      "priority": 22,
      "passes": false,
      "notes": "Reference mockup: docs/mockups/04-shortlists-page.png"
    },
    {
      "id": "US-022",
      "title": "Re-scrape support",
      "description": "Refresh Data button on project page. Re-fetches Place Details for all existing places to update data. Skips tiling/discovery. Special ScrapeRun type. Progress shown in UI.",
      "acceptanceCriteria": [
        "'Refresh Data' button on project Setup page",
        "POST /api/scrape/rescrape accepts { projectId } and creates a re-scrape run",
        "Re-scrape iterates existing place IDs, calling Place Details API for each",
        "Updated place data (rating, reviews, price, etc.) overwrites old data",
        "scrapedAt timestamp updated for each re-scraped place",
        "Progress shown via SSE (places refreshed / total)",
        "npm run typecheck --workspace=server passes"
      ],
      "priority": 23,
      "passes": false,
      "notes": "Uses Place Details API instead of re-visiting browser pages."
    },
    {
      "id": "US-023",
      "title": "External search links (Booking.com, Airbnb)",
      "description": "In detail panel, three full-width blue action buttons: 'Open in Google Maps', 'Search on Booking.com', 'Search on Airbnb'. Encode place name + location into OTA search URLs. Open in new tab.",
      "acceptanceCriteria": [
        "Three full-width blue buttons at bottom of detail panel with icons per mockup",
        "'Open in Google Maps' button opens googleMapsUri in new tab",
        "'Search on Booking.com' button encodes place name + location into Booking.com search URL",
        "'Search on Airbnb' button encodes place name + location into Airbnb search URL",
        "All links open in new tab (target=_blank)",
        "Buttons only shown when place has a name and location",
        "npm run typecheck --workspace=client passes"
      ],
      "priority": 24,
      "passes": false,
      "notes": "Reference mockup: docs/mockups/03-explorer-page.png"
    },
    {
      "id": "US-024",
      "title": "Stale data indicators",
      "description": "Visual indicator in table and detail panel for places with scrapedAt > 7 days old. Configurable threshold. 'Show stale only' toggle in filter panel.",
      "acceptanceCriteria": [
        "Places with scrapedAt older than threshold show stale indicator (icon + tooltip) in table",
        "Stale indicator also shown in detail panel",
        "Threshold configurable (default: 7 days)",
        "'Show stale only' toggle added to filter panel",
        "Stale filter integrates with existing filter system",
        "npm run typecheck --workspace=client passes"
      ],
      "priority": 25,
      "passes": false,
      "notes": ""
    }
  ]
}
